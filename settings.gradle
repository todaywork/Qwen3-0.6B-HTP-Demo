pluginManagement {
    repositories {
        google()
        mavenCentral()
        gradlePluginPortal()
    }
}

dependencyResolutionManagement {
    repositoriesMode.set(RepositoriesMode.FAIL_ON_PROJECT_REPOS)
    repositories {
        google()
        mavenCentral()
    }
}

rootProject.name = "Qwen3HtpDemo"
include ":app"

def enableNpuDiagnostics = providers.gradleProperty("enableNpuDiagnostics")
        .map { it.toBoolean() }
        .orElse(true)
        .get()
if (enableNpuDiagnostics) {
    include ":npu-diagnostics"
}
